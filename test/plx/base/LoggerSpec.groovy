import plx.base.Logger

import com.homeaway.devtools.jenkins.testing.JenkinsPipelineSpecification

import spock.lang.*

class LoggerSpec extends Specification {

    def setup () {}
    /*
    def "Plx.Logger_info" () {
        when:
            Logger.instance.info("Message")
        then:
            true
        expect:
            true
    }

    def "Plx.Logger_error" () {
        when:
            Logger.instance.error("Message")
        then:
            true
        expect:
            true
    }

    def "Plx.Logger_console" () {
        when:
            Logger.instance.console("Message")
        then:
            true
        expect:
            true
    }
    */
}